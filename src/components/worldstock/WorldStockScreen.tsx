import React from "react";

interface Props {}

const WorldStockScreen = ({}: Props): React.ReactNode => {
  return (
    <div>
      <div>WorldStockScreen</div>
    </div>
  );
};

export default WorldStockScreen;
