export interface WorldStockScreenRouter {
}

const useRouter = (): WorldStockScreenRouter => {
  // implement methods in router interface to navigate to other screens.
  return {};
};

export default useRouter;
